name: Coach Bag Watcher

# DISABLED 2026-08-16. Coach bag checks are off. Only the open-box G7 X
# (SKU 14350718) is watched. File kept so history stays. No schedule.
# workflow_dispatch remains if someone intentionally re-enables later.

on:
  workflow_dispatch:
    inputs:
      simulate:
        description: "DISABLED. Do not run unless you intentionally re-enable Coach."
        required: false
        default: "false"

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Coach watcher is disabled. Open-box G7X only."
